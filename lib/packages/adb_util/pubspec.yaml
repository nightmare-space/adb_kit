name: adb_util
description: "A new Flutter package project."
version: 0.0.1
homepage: none

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=1.17.0"

resolution: workspace

dev_dependencies:
  test: ^1.25.7

dependencies:
  # global_repository:
  # signale:
  # android_api_server_client:
  # crypto:
  flutter:
    sdk: flutter
