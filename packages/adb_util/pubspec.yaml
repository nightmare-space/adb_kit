name: adb_util
description: "A new Flutter package project."
version: 0.0.1
homepage: none

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=1.17.0"

resolution: workspace

dev_dependencies:
  test: ^1.25.7

dependencies:
  # global_repository:
  # signale:
  # android_api_server_client:
  # crypto:
  # adb_kit:
  flutter:
    sdk: flutter
  dart_adb:
    path: /Users/lori/Desktop/nightmare-space/dart_adb
  multicast_dns: ^0.3.2+8
