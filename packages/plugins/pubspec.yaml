name: plugins
description: "A new Flutter package project."
version: 0.0.1
homepage: none
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=1.17.0"

resolution: workspace
## Failed to ensure /data/user/0/com.termux.api/shared_prefs

dependencies:
  flutter:
    sdk: flutter
  adb_kit:
  get:
  adb_util:
  responsive_framework:
  android_api_server_client:
  flutter_svg:
  global_repository:
  file_manager:
  intl:
  animations:
  file_selector:
  flutter_pty:
  window_manager:
  desktop_drop:
  flutter_spinkit:
  path:
  dio:
  signale:
  adb_interface:
  flutter_localizations:
    sdk: flutter
  android_tool:
    path: /Users/nightmare/Desktop/nightmare-space/android_tool
