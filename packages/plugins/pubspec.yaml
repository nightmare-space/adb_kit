name: plugins
description: "A new Flutter package project."
version: 0.0.1
homepage: none
publish_to: none

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

resolution: workspace
## Failed to ensure /data/user/0/com.termux.api/shared_prefs

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

flutter_intl:
  enabled: true
  class_name: P
